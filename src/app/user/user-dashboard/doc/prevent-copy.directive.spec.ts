import { PreventCopyDirective } from './prevent-copy.directive';

describe('PreventCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventCopyDirective();
    expect(directive).toBeTruthy();
  });
});
